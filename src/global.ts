export const chohan: string[] = ["丁", "半"];

export const isInclude =
  (min: number, judge: number, max: number): boolean =>
    min <= judge && max >= judge;

export const npcNames = [
  "KEN", "SAKURA", "MOMO", "RYUJI", "LIKO", "SOTA"
];

export const flexCenter = {
  display: "flex",

  justifyContent: "center",

  flexDirection: "column"
};
